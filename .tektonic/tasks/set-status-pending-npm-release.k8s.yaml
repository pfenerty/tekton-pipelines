apiVersion: tekton.dev/v1
kind: Task
metadata:
  name: tektonic-set-status-pending-npm-release
  namespace: tektonic-ci
spec:
  params:
    - name: repo-full-name
      type: string
    - name: revision
      type: string
  stepTemplate:
    computeResources:
      limits:
        cpu: "1"
        memory: 512Mi
      requests:
        cpu: 100m
        memory: 128Mi
    securityContext:
      allowPrivilegeEscalation: false
      capabilities:
        drop:
          - ALL
  steps:
    - env: []
      image: ghcr.io/pfenerty/apko-cicd/base:stable
      name: pending-publish-npm
      onError: continue
      script: |-
        #!/usr/bin/env nu
        def log [msg: string] { print $"[(date now | format date '%H:%M:%S')] ($msg)" }
        let url = $"https://api.github.com/repos/$(params.repo-full-name)/statuses/$(params.revision)"
        let body = { state: "pending", context: "publish-npm", description: "Running" }

        log $"status-pending [publish-npm]: POST ($url)"
        log $"status-pending [publish-npm]: body: ($body | to json -r)"

        try {
          http post $url $body -t application/json -H [
            Authorization $"token ($env.GITHUB_TOKEN)"
            Accept "application/vnd.github+json"
          ]
          log "status-pending [publish-npm]: done"
        } catch { |e|
          log $"status-pending [publish-npm]: error: ($e.msg)"
          exit 1
        }
    - env: []
      image: ghcr.io/pfenerty/apko-cicd/base:stable
      name: pending-build-npm
      onError: continue
      script: |-
        #!/usr/bin/env nu
        def log [msg: string] { print $"[(date now | format date '%H:%M:%S')] ($msg)" }
        let url = $"https://api.github.com/repos/$(params.repo-full-name)/statuses/$(params.revision)"
        let body = { state: "pending", context: "build-npm", description: "Running" }

        log $"status-pending [build-npm]: POST ($url)"
        log $"status-pending [build-npm]: body: ($body | to json -r)"

        try {
          http post $url $body -t application/json -H [
            Authorization $"token ($env.GITHUB_TOKEN)"
            Accept "application/vnd.github+json"
          ]
          log "status-pending [build-npm]: done"
        } catch { |e|
          log $"status-pending [build-npm]: error: ($e.msg)"
          exit 1
        }
    - env: []
      image: ghcr.io/pfenerty/apko-cicd/base:stable
      name: pending-test-npm
      onError: continue
      script: |-
        #!/usr/bin/env nu
        def log [msg: string] { print $"[(date now | format date '%H:%M:%S')] ($msg)" }
        let url = $"https://api.github.com/repos/$(params.repo-full-name)/statuses/$(params.revision)"
        let body = { state: "pending", context: "test-npm", description: "Running" }

        log $"status-pending [test-npm]: POST ($url)"
        log $"status-pending [test-npm]: body: ($body | to json -r)"

        try {
          http post $url $body -t application/json -H [
            Authorization $"token ($env.GITHUB_TOKEN)"
            Accept "application/vnd.github+json"
          ]
          log "status-pending [test-npm]: done"
        } catch { |e|
          log $"status-pending [test-npm]: error: ($e.msg)"
          exit 1
        }
